# a comment
! a comment

#webserver = jetty or tomcat
Webserver = tomcat

# Protocol = https or http
Protocol = https

#HTTPVersion = 1.1 or 2
HTTPVersion = 1.1

#TLSVersion = TLSv1.2 or TLSv1.1 or TLSv1 or a list separated by colon -> TLSv1.2,TLSv1.1,TLSv1
TLSVersion = TLSv1.2

Port = 8443

# Servlet generic parameters
ServletContext = /iMonserver
ServletPath = /App
ServletTmpDir = java.io.tmpdir
ServletName = iMonserver



KeystorePassword = changeit
# Parameters currently used only by Jetty
Keystore = /root/.keystore 